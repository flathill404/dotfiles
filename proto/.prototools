[settings]
auto-install = true

# Version ranges: ~N means "latest patch within major N"
# Use `proto pin <tool> <major>` to update these when bumping major versions.
node = "~24"
pnpm = "~10"
python = "~3.13"
