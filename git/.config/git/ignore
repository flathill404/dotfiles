# ── macOS ────────────────────────────────────────────────────────────────────
.DS_Store
._*
.Trash-*

# ── Editors ──────────────────────────────────────────────────────────────────
*.swp
*.swo
*~

# ── Secrets & Credentials (defense in depth) ─────────────────────────────────
.env
.env.*
# Keep committable templates visible — these are meant to be tracked
# (onboarding files). Real secrets like .env.local stay ignored above.
!.env.example
!.env.sample
!.env.template
!.env.dist
!.env.defaults
!.env.*.example
.envrc
*.pem
*.key
*.p12
*.pfx
id_rsa*
id_ed25519*
id_ecdsa*
credentials.json
service-account*.json
.gcp-credentials.json
*.keystore
*.jks
.secret
.secrets/
.npmrc
.pypirc
.netrc

# ── Claude Code ──────────────────────────────────────────────────────────────
**/.claude/settings.local.json
